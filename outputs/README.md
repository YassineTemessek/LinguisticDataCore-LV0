# Outputs (local)

Generated artifacts (manifests, release zips, previews) go here and are ignored by Git.

Typical folders:

- `outputs/manifests/`: ingest run manifests
- `outputs/release_assets/<YYYY.MM.DD>/`: per-language zips + manifest JSON

